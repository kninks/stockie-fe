import styles from './styles/Footer.module.css';

const Footer = () => {
    return (
        <footer className={styles.container}>
            <div>Stockie</div>
            <div>Second</div>
        </footer>
    );
};

export default Footer;
