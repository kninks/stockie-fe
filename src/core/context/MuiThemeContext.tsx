import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    typography: {
        fontFamily: `'Sarabun', 'Inter', 'Open Sans', sans-serif`,
    },
});

export default theme;
