// import api from "./axiosInstance";

// Fetch the stock prediction based on industry & days
// export const fetchPrediction = async (industry: string, days: number): Promise<PredictResult> => {
// //     const response = await api.get(`/predict?industry=${industry}&days=${days}`);
// //     return response.data;
// // };